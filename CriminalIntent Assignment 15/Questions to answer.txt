1. How do you  return a thumb nail picture? Answer should be the type of object you insert the picture and the method name that returns the object.
ACTION_IMAGE_CAPTURE will take the picture then stick inside the Intent object returned in the onActvityResult.
2. What is the function to scale a photo to a Bitmap?
getScaledBitmap scales the bitmap image to a BitMap.
3. What is the function to load Bitmap into ImageView?
The UpDatePhotoView function loads the Bimap into the imageView.The function first gets the scaled bitmap using the getScaledBitmap function and 
then loads it to the imageView with setImageBitmap.